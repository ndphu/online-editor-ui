{
  "name": "online-editor-ui",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "draft-js": "^0.11.7",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-draft-wysiwyg": "^1.14.7",
    "react-router-dom": "^5.0.0",
    "react-scripts": "4.0.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy:gae": "cross-env REACT_APP_BUILD_TARGET=gae react-scripts build && firebase deploy --only hosting",
    "deploy:heroku": "cross-env REACT_APP_BUILD_TARGET=heroku react-scripts build && firebase deploy --only hosting",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
